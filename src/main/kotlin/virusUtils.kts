import java.util.math

val replicationRate = 100.0
val virusStrength = 0.01
val complicationsStrength = 10.0
val mutationRate = 1.0 * pow(10, -6)

fun mutate(strain: String): String {
    // TODO
    return strain
}

fun determineSimilarity(strainA: String, strainB: String) {
    // TODO
    return 1.0
}